public enum WeaponPower
{
    no,
    poison,
    stun,
    slowing
}

public enum ItemType
{
    Armour,
    Consumable,
    Weapon,
    Misc
}

public enum RendererType
{
    UI,
    Sprite
}

public enum Direction
{
    Up,
    Down,
    Left,
    Right
}
